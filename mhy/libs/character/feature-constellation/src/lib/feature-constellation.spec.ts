import { featureConstellation } from './feature-constellation';

describe('featureConstellation', () => {
  it('should work', () => {
    expect(featureConstellation()).toEqual('feature-constellation');
  });
});
